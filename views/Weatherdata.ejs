<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Weather Report - <%= location.name %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Poppins", sans-serif;
        background-color: #f0fdf4;
      }
      .weather-card {
        backdrop-filter: blur(10px);
        background: rgba(255, 255, 255, 0.9);
      }
      .glass-effect {
        background: rgba(255, 255, 255, 0.8);
        backdrop-filter: blur(10px);
      }
    </style>
  </head>
  <body class="min-h-screen">
    <%- include('partials/header') %>

    <div class="pt-20 pb-12 px-4">
      <div class="max-w-7xl mx-auto">
        <!-- Location Header -->
        <div class="text-center mb-8">
          <h1 class="text-4xl font-bold text-gray-800 mb-2">
            <%= location.name %>, <%= location.region %>
          </h1>
          <p class="text-lg text-gray-600"><%= current.last_updated %></p>
        </div>

        <!-- Main Weather Card -->
        <div class="weather-card rounded-2xl shadow-xl p-8 mb-8">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
            <!-- Current Weather -->
            <div class="text-center md:text-left">
              <div
                class="flex items-center justify-center md:justify-start mb-6"
              >
                <img
                  src="<%= current.condition.icon %>"
                  alt="<%= current.condition.text %>"
                  class="w-24 h-24"
                />
                <div class="ml-4">
                  <h2 class="text-5xl font-bold text-gray-800">
                    <%= current.temp_c %>°C
                  </h2>
                  <p class="text-xl text-gray-600">
                    <%= current.condition.text %>
                  </p>
                </div>
              </div>
              <p class="text-gray-600">
                Feels like <%= current.feelslike_c %>°C
              </p>
            </div>

            <!-- Key Metrics -->
            <div class="grid grid-cols-2 gap-6">
              <div class="glass-effect p-4 rounded-xl">
                <p class="text-gray-600 text-sm">Humidity</p>
                <p class="text-2xl font-semibold text-gray-800">
                  <%= current.humidity %>%
                </p>
              </div>
              <div class="glass-effect p-4 rounded-xl">
                <p class="text-gray-600 text-sm">Wind</p>
                <p class="text-2xl font-semibold text-gray-800">
                  <%= current.wind_kph %> km/h
                </p>
                <p class="text-sm text-gray-600"><%= current.wind_dir %></p>
              </div>
              <div class="glass-effect p-4 rounded-xl">
                <p class="text-gray-600 text-sm">Precipitation</p>
                <p class="text-2xl font-semibold text-gray-800">
                  <%= current.precip_mm %> mm
                </p>
              </div>
              <div class="glass-effect p-4 rounded-xl">
                <p class="text-gray-600 text-sm">UV Index</p>
                <p class="text-2xl font-semibold text-gray-800">
                  <%= current.uv %>
                </p>
              </div>
            </div>
          </div>
        </div>

        <!-- Detailed Information -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          <!-- Pressure Card -->
          <div class="bg-white rounded-xl shadow-lg p-6">
            <h3 class="text-lg font-semibold text-gray-800 mb-4">Pressure</h3>
            <div class="flex justify-between items-center">
              <span class="text-3xl font-bold text-gray-800"
                ><%= current.pressure_mb %></span
              >
              <span class="text-gray-600">mb</span>
            </div>
          </div>

          <!-- Visibility Card -->
          <div class="bg-white rounded-xl shadow-lg p-6">
            <h3 class="text-lg font-semibold text-gray-800 mb-4">Visibility</h3>
            <div class="flex justify-between items-center">
              <span class="text-3xl font-bold text-gray-800"
                ><%= current.vis_km %></span
              >
              <span class="text-gray-600">km</span>
            </div>
          </div>

          <!-- Cloud Cover Card -->
          <div class="bg-white rounded-xl shadow-lg p-6">
            <h3 class="text-lg font-semibold text-gray-800 mb-4">
              Cloud Cover
            </h3>
            <div class="flex justify-between items-center">
              <span class="text-3xl font-bold text-gray-800"
                ><%= current.cloud %></span
              >
              <span class="text-gray-600">%</span>
            </div>
          </div>
        </div>

        <!-- Additional Details -->
        <div class="mt-8 bg-white rounded-xl shadow-lg p-6">
          <h3 class="text-xl font-semibold text-gray-800 mb-4">
            Additional Information
          </h3>
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            <div>
              <p class="text-gray-600">Heat Index</p>
              <p class="text-lg font-semibold"><%= current.heatindex_c %>°C</p>
            </div>
            <div>
              <p class="text-gray-600">Dew Point</p>
              <p class="text-lg font-semibold"><%= current.dewpoint_c %>°C</p>
            </div>
            <div>
              <p class="text-gray-600">Wind Gust</p>
              <p class="text-lg font-semibold"><%= current.gust_kph %> km/h</p>
            </div>
            <div>
              <p class="text-gray-600">Local Time</p>
              <p class="text-lg font-semibold"><%= location.localtime %></p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
